# Pending dense work after ticket 10

These are implementation notes, not accepted capabilities. Do not mark later tickets complete before their real phase tests.

- `tests/create_torchtitan_gqa_reference.py` now accepts `DEEPSPEC_GQA_REFERENCE_WORKERS` (default two), so the retained trainer can independently archive matching DP4/DP8 objectives. The new worker counts have not been run. DP8 references can serve replicate8, shard8 and HSDP2x4; their logical sample grouping differs from the current DP2 references.
- Pure shard8 can use the existing one-axis native FSDP path. Replicate8/HSDP2x4 need the existing native SPMD-to-DTensor storage bridge on `dp_replicate, dp_shard`, with replicated placements and local Linear/Embedding/Norm computation. For TP1 the TP group must be absent and Q/K head-gradient reduction disabled. Native `resolve_fsdp_mesh` already specifies `DataParallelMeshDims(shard=dp_shard, replicate=dp_replicate)`; FSDP gradient division must remain disabled. Do not substitute the legacy partial-DTensor path.
- The current numerical entry/checkpoint fixtures assume DP one/two and two GAS microbatches. Generalize only as new matrix rows require, keeping archived DP2 fixtures and their comparison semantics unchanged.
- SP candidates must account for both context tokens and anchor-query tokens. The current two-anchor/block-three fixture has six query tokens, which do not divide TP4. A reference with four anchors would have twelve; any layout restriction must be explicit rather than silently changing the user's anchor count.
- Native PP includes `num_pp_microbatches` in the per-update token budget. Pipeline microbatch averaging and outer GAS must jointly preserve each logical microbatch's separate global denominator. Do not compare a different grouping to the current GAS-two objective.

## Fixture preparation at 22:33 (not new topology acceptance)

- `test_torchtitan_phase_training.py` now permits logical DP worker counts 1/2/4/8. Every DP count greater than one uses its matching archived retained reference and removes the archived DP compensation exactly once. The default DP1/DP2 behavior is unchanged.
- `torchtitan_phase_fixtures.fixed_features` accepts `DEEPSPEC_PHASE_DP_REPLICATE` (default one), validates divisibility, and sets DP shard count to physical workers / TP / replicas. Logical sample grouping and token budget still use total DP workers. No runtime topology support was added yet.
- `test_torchtitan_phase_checkpoint.py` accepts `DEEPSPEC_PHASE_WORKERS` (default two logical DP workers), keeping GAS two and each phase's microbatch/update boundaries unchanged. All per-rank fixtures, launches and trajectory comparisons use the full worker count.
- `test_torchtitan_phase_failure.py` checks the actual archived request's worker count before launch rather than assuming two GPUs. The failure remains a real DCP payload/metadata write followed by failed marker rename.
- Ruff checks passed. New DP4/DP8/HSDP GPU cases have not run; do not mark 11/12 complete. These fixture-only edits do not affect the active full-scale recipe.

- At22:42 prepared an isolated two-file candidate in `dense-candidate/` plus `candidate.patch`; active runtime was not edited. It generalizes the accepted n-D storage bridge to DP replicate/HSDP, with TP1 replicated linears and no head-gradient TP reduction. The existing TP>1 branch stays the same. Ruff passes; no GPU validation. Rebase/apply only after ticket10 baseline is accepted, then run matching retained DP8 reference and every requested phase/failure case before claims.

## SP/CP/PP source observations (not implementation)

- The projected teacher context is constant across all decoder layers: `_forward_backbone` computes `hidden_norm(fc(target_hidden_states))` once, then every layer consumes it while only the query/residual stream changes. SP must address both this long context and the shorter anchor-query stream.
- Existing TP ColumnLinear backward already reconstructs the complete hidden gradient across heads. A sequence gather around it must scatter that complete gradient back to local tokens, not reduce-scatter SUM and multiply it by TP again. Row-to-sequence slicing has the dual gather in backward. Sequence-local replicated norm/linear weight gradients need their full token contraction exactly once; normal TP head-norm rules are different and must stay distinct. Do not silently change two anchors merely because six query tokens do not divide TP4; either explicitly support uneven activation padding/trimming or use a separately documented divisible reference.
- Native CP can reuse the retained DSpark ring attention already owned in model.py. `DSparkDraftModel.preprocess_inputs` is the model-owned seam for head/tail views of `target_hidden_states` and `target_last_hidden_states`; preserve full input_ids/loss_mask and original seq_len, and set local context_chunk_len to2*ceil(length/(2*CP)). `features.context_positions` already describes this layout. No producerchange isneeded.
- `resolve_fsdp_mesh` folds CP into the parameter-sharding axes, and the native loss mesh includesDP+CP while excludingTP. Configure the DSpark CP group and the flattened CP+TP anchor-broadcast group explicitly. Guard outer compile as before.
- PP requires a real split of the draft interface: stage0 owns differentiable teacher projection and embedding/early layers; stage1 owns later layers/norm/heads. Both query and projected context gradients must traverse the boundary. Merely splittingdecoderlayers isinsufficient. Native model_spec.pipelining_fn iscurrentlyNone. PP tests needfiveactualDSparklayers andmatchingwhole-logical-microbatchreference; stage-localinitialweights andfrozen-targetloading mustfilterownership ratherthanrequiringallglobalkeys on every stage.
